import React from 'react';
import { AppRegistry } from 'react-native';
import AppRootContainer from './App/AppRootContainer';

const SwamphacksMobile1 = () => (
  <AppRootContainer />
)

AppRegistry.registerComponent('SwamphacksMobile1', () => SwamphacksMobile1);
